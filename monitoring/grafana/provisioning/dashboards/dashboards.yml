apiVersion: 1

providers:
  - name: 'Copilot Chat Backup'
    orgId: 1
    folder: 'Copilot Chat Backup'
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
